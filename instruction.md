Bring the log-shipping deployment back online after a failed log-shipping rollout. The rollout left the responder host in a bad state: the dedicated service account, the operator wrapper at `/usr/local/bin/ship-logs`, the compile lock under `/var/lock`, the cron schedule, the output directory ownership and permissions, the compile log directory under `/var/log/log-shipper` and its rotation drop-in are all missing or wrong, and an unrotated leftover from the crashed run is still sitting on disk. `/app/docs/deployment_runbook.md` defines the deployment state you need to restore: provision the account with no interactive shell, reinstate the schedule under that account, correct the ownership and modes, prune the rollout leftover, clear the stale lock, and install rotation so the scheduled compile both runs and rotates as the service account rather than as root.

The same rollout shipped a compiler build that produces unsafe responder queues, so restoring the host is not enough on its own. Return `/app/workflow/log_ship.py` to the behaviour the change-advisory board signed off, keeping its interface `--input PATH --output-dir PATH` with defaults `/app/data/batches.json` and `/app/output`, so that it writes `shipment_summary.json`, `lag_windows.json` and compact `retry_queue.jsonl`. It has to stay idempotent and keep working on a different batch input. `/app/docs/shipping_contract.json` is the output contract — source paths, schemas, exact key sets, identifier payloads and checksum encodings — and your output must match it exactly.

How the compile is meant to behave is not in the contract. The change-advisory board settled it, and `/app/batch/shipping_review_log.md` is the record: normalization, dedupe, attenuation, the debt ledger, scoring, priority and ordering are all LOG-ticketed entries in there. Where two entries disagree, the later dated decision governs. Leave `/app/workflow/.log_ship.original` unchanged. The repaired compiler reads only its declared inputs under `/app/data` and derives every value from those inputs and the board's rulings; it must not read from outside the application or embed precomputed results.

The review log mixes reversed February drafts with the governing 2026-05 CAB decisions. The contract's `governing_entry_index.final_governing_decisions` lists exactly the LOG tickets that finally govern (superseded drafts excluded) — apply those and ignore the drafts; grep each ticket for its rule, and where two governing decisions overlap the later dated one wins.
